[package]
name = "thoth-mesh"
description = "Federation and gossip layer for the thoth-mesh federated pub/sub system: peer discovery and replication."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
thoth-mesh-core = { path = "../thoth-mesh-core", version = "0.1.0", features = ["async"] }
futures-util = { version = "0.3.33", default-features = false, features = ["std", "io"] }
thiserror = "2.0.19"
tracing = "0.1.44"

[dev-dependencies]
futures-executor = "0.3.33"
